define([ 'jQuery', 'backbone', 'backbone.marionette' ], function($,
                                                                 Backbone,
                                                                 Marionette) {
    var test = Backbone.Model.extend({});
    var test2 = Marionette.ItemView.extend({});
    alert('Hello World 2');
    return;
});